Steps to get the build running on localhost
1. install npm and node if you haven't already https://docs.npmjs.com/downloading-and-installing-node-js-and-npm
2. run "npx serve -s build" in project directory 
